To run the entire project
1. Go to {android-sdk}/extras/google/google_play_services/libproject
	- Create a new Android project by selecting the option - Create project from existing code and use google-play-services_lib
		This will define a library for the google play which we will require to use its APIs
	- Go to project properties -> Android -> add this library
